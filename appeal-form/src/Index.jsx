import React from 'react';
import ReactDOM from 'react-dom/client';
import './BoibyUI.css';
import App from './AppealFormApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);